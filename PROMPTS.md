# 체크포인트 프롬프트 4개

오후 실습(13:00–16:00)용. **순서대로** Claude Code에 붙여넣으세요.
각 단계는 그 자체로 발표 가능한 산출물을 냅니다 — 어디서 멈춰도 발표할 게 있습니다.

> **시작 전 확인**
> `python3 -m http.server 8000` 실행 → 브라우저에서 `localhost:8000` 이 열리고
> "그래프 적재 완료" 가 보이면 준비 끝입니다.

---

## ① 수집·전처리 (13:20–13:50 · 30분)

> 산출물: `data/corpus.txt`, `data/segments.jsonl`

```
내 구술자의 기록으로 이 프로젝트를 바꾸려고 한다.

준비한 파일: [여기에 파일명을 적으세요. 예: 김원기_구술총서.pdf]
구술자: [이름]

다음을 해 줘.
1. 파일에서 텍스트를 뽑아 data/corpus.txt 로 저장. 쪽 머리글·꼬리글·각주 기호는 제거.
2. 문답 단위로 잘라 data/segments.jsonl 로 저장.
   각 줄은 {"id","chapter","page","speaker","text"} 형식.
   speaker 는 "면담자" 또는 "구술자" 로 구분.
3. 몇 개 세그먼트가 나왔는지, 쪽 범위는 어디까지인지 알려 줘.

중요: 쪽 번호를 반드시 보존할 것. 나중에 모든 트리플의 출처 근거가 된다.
텍스트가 안 뽑히는 스캔 PDF면 그 사실을 알려 주고 멈춰. OCR은 시간이 부족하니
그 경우 data/corpus.txt 에 있는 정세균 샘플로 계속 진행한다.
```

---

## ② 온톨로지 정의 (13:50–14:20 · 30분)

> 산출물: `data/ontology.rdf` 확인, 전거 마스터 확장

```
data/ontology.rdf 에 있는 RiC-O 구술 프로파일을 읽고 다음을 해 줘.

1. 이 프로파일의 클래스와 속성을 표로 정리해서 보여 줘.
   각 속성의 도메인과 레인지를 반드시 포함할 것.
2. 내 구술자를 기술하는 데 이 12개 클래스로 충분한지 검토해 줘.
   부족하면 RiC-O 1.1 원본에 실재하는 속성 중에서만 추가 후보를 제안해.
   (없는 속성을 지어내지 말 것)
3. data/authority.json 에서 내 구술자를 찾아 재임 이력을 확인해 줘.
   없으면 그 사실을 알려 줘.

그리고 설명해 줘 — 왜 인물을 단체에 바로 연결하지 않고 '직위'를 거치는가?
내 구술자의 경우 어떤 직위들이 필요한가?
```

---

## ③ 추출·그라운딩 (14:20–15:05 · 45분) ★ 핵심

> 산출물: `data/graph.ttl`, `data/validation-report.md`

```
data/segments.jsonl 을 읽고 지식그래프를 만들어 줘.

추출:
- 각 세그먼트에서 인물·단체·직위·사건·장소를 뽑아라.
- 각 개체에 RiC-O 클래스를 배정하라. data/ontology.rdf 의 12개 클래스만 쓴다.
- 개체 간 관계를 트리플로 만들어라. 프로파일의 속성만 쓴다.

그라운딩:
- 인물은 data/authority.json 의 전거 마스터와 대조하라.
  매칭되면 그 ID를 쓰고, 안 되면 ric:local-<이름> 으로 두고 '신규 후보'로 표시하라.
- 모든 트리플에 출처 쪽수를 주석으로 달아라.

검증 (아래 6종을 전부 돌리고 결과를 data/validation-report.md 에 써라):
  1. 클래스 실재 — 프로파일에 있는 클래스인가
  2. 도메인 위반 — 주어 클래스가 그 속성의 rdfs:domain 에 속하는가
  3. 레인지 위반 — 목적어 클래스가 rdfs:range 에 속하는가
  4. 전거 미매칭 — 몇 명이 매칭되고 몇 명이 신규 후보인가
  5. 날짜 정합성 — 형식이 맞고 재임기간 범위 안인가
  6. 출처 앵커 — 쪽수 없는 트리플이 있는가

검증에 실패한 트리플은 graph.ttl 에 넣지 말고 리포트에만 남겨라.
마지막에 rdflib 로 graph.ttl 을 파싱해서 문법이 맞는지 확인하고 트리플 수를 알려 줘.

주의: Turtle 지역명에 '/' 를 쓰면 파싱이 깨진다. 하이픈을 써라.
```

**이 단계가 끝나면 확인하세요**
- 검증 실패가 몇 건인가? 어떤 규칙에 걸렸나?
- 그럴듯한데 틀린 트리플이 있었나? 왜 틀렸나?

---

## ④ 시맨틱 검색·시각화 (15:15–15:45 · 30분)

> 산출물: 동작하는 6개 섹션

```
새 graph.ttl 로 화면을 갱신해 줘.

1. 지도 — data/places.csv 를 내 구술자의 장소로 바꾸고 좌표를 채워라.
   좌표를 모르면 추측하지 말고 비워 두고 알려 줘.
2. 연표 — data/events.csv 를 내 구술자의 사건으로 바꿔라.
3. 관계망 — 그래프에서 자동으로 그려지니 확인만 하고, 노드가 너무 많으면
   필터 기본값을 조정해라.
4. 언어 — corpus.txt 가 바뀌었으니 불용어를 내 자료에 맞게 손봐라.
5. 해설 — 우리 팀의 핵심 질문 한 문장과 그 답을 써라.

그리고 이 그래프에 물어볼 만한 질문 5개를 만들어서
질의 섹션의 예시 버튼(chips)을 우리 데이터에 맞게 바꿔 줘.
각 질문이 실제로 결과를 내는지 SPARQL로 미리 확인하고, 결과가 0건인 질문은 빼라.
```

---

## 발표 준비 (15:45–16:00)

```
발표용으로 다음을 정리해 줘.
1. 우리 구술자는 누구이고 왜 이 인물을 골랐는가 (2문장)
2. 우리가 답하려 한 질문 한 문장
3. 그래프 규모 — 개체 수, 트리플 수, 전거 매칭률
4. 검증에서 걸러낸 오류 중 가장 인상적인 것 하나와 그 이유
5. 그래프 없이 물었을 때 vs 그래프로 물었을 때의 답 차이

그리고 GitHub Pages 로 배포하는 방법을 알려 줘.
시간이 부족하면 로컬 화면 스크린샷으로 대체한다.
```

---

## 막힐 때

| 증상 | 이렇게 말하세요 |
|---|---|
| TTL 파싱 오류 | `이 에러 그대로야: [붙여넣기]. graph.ttl 문법을 고쳐 줘` |
| 그래프가 안 뜸 | `브라우저 콘솔 에러야: [붙여넣기]. 원인 찾아 줘` |
| SPARQL 결과 0건 | `이 질의가 0건이야: [질의]. 그래프에 실제로 뭐가 있는지 보여 줘` |
| 노드가 너무 많음 | `관계망 노드를 30개 이하로 줄이는 필터를 넣어 줘` |
| API 오류 | `API 응답이야: [붙여넣기]` (키 자체는 절대 붙여넣지 말 것) |
