# 오후 실습 — 서비스킷 차례

2026 온톨로지·지식그래프 실습(2026. 8. 19.)의 **15:30–16:00 「탐색」** 과 그 심화에서 씁니다.

앞 시간(13:00–15:30)에 만든 것을 그대로 이어받습니다.

```
AP 설계(그라운더·관리시스템)  →  8단계 그라운딩  →  .ttl 내려받기  →  ★ 여기부터
```

원문에서 개체를 뽑고 전거·시소러스에 매핑하는 일은 **그라운더(또는 구술기록관리시스템)**가 합니다.
서비스킷은 그 결과인 `.ttl` 을 **화면으로 만드는** 자리입니다.

---

## 0. 올려서 확인 (5분 · 코딩 없음)

1. https://archivelabedu.github.io/ontology-starterkit/#collection
2. **컬렉션 이름**을 적고(예: `3팀 박희태 구술`) `.ttl` 고르기
3. 검색·지도·연표·관계망·주제·질의로 돌아보기

여기서 **비어 보이는 화면이 곧 할 일 목록**입니다. 지도가 비면 좌표가 없는 것이고,
연표가 비면 날짜가 없는 것이며, 주제가 비면 시소러스 매핑이 빠진 것입니다.
그라운더로 돌아가 채우고 다시 올리면 됩니다.

> 올린 파일은 **내 브라우저에만** 남습니다. 발표는 각자 노트북 화면으로 합니다.

---

## 1. 내 사이트로 만들기 (심화 · 20분~)

포크·클론한 폴더에서 `claude` 를 실행하고 아래를 순서대로 붙여 넣습니다.
`CLAUDE.md` 가 자동으로 읽혀 규칙(등재 어휘만·모든 트리플에 출처·PN_LOCAL)이 지켜집니다.

### ① 데이터 갈아끼우기

```text
data/graph.ttl 을 우리 팀 TTL 로 교체했습니다.
1) 화면 여덟 개(기록·지도·연표·관계망·주제·질의·언어·컬렉션)가 새 데이터로 그려지는지 확인하고,
   비어 보이는 화면은 원인이 데이터 부족인지 코드 가정인지 구분해 알려 주세요.
2) data/places.csv · data/events.csv 를 새 그래프에서 다시 만들어 주세요.
   좌표나 날짜가 없는 것은 지어내지 말고 "없음" 목록으로 남겨 주세요.
3) 사이트 제목·부제·푸터의 기관명을 "<우리 기관/구술자>" 로 바꿔 주세요.
검증: python3 -m http.server 8000 으로 띄워 콘솔 오류 0, 화면별 개체 수를 보고해 주세요.
```

### ② 우리 기관 색으로

```text
index.html 의 CSS 변수만 고쳐 우리 기관 색으로 바꿔 주세요.
--accent 를 <#색상> 으로 하고, 그 색과 어울리는 중립 회색·선 색을 함께 정해 주세요.
다크 모드에서도 대비가 유지되어야 하고, 색을 하드코딩한 곳이 남지 않아야 합니다.
바꾸기 전후를 같은 화면에서 비교해 보여 주세요.
```

### ③ 우리 기록에 맞는 화면 하나 더

```text
우리 구술에는 <예: 회의록 발언 · 사진 · 지역 활동> 이 많습니다. 이를 위한 화면을 추가해 주세요.
- 메뉴에 항목을 넣고 #<이름> 해시로 열리게 하고, route() 의 재계산 목록에도 등록할 것
- 데이터는 graph.ttl 에서 SPARQL 로만 가져올 것(하드코딩 금지)
- 라이브러리 추가 금지 — 캔버스/SVG 로 직접 그릴 것
- 데이터에 없는 값은 만들지 말고 "없음" 으로 둘 것
```

### ④ 배포

```text
GitHub Pages 로 배포하는 절차를 알려 주고, 배포 전에 확인할 것을 점검해 주세요.
- data/ 에 API 키나 개인정보가 섞이지 않았는지
- 이미지 출처 표기가 모두 남아 있는지
- 첫 화면이 우리 팀 데이터로 뜨는지
```

---

## 2. 발표 준비 (15:45–16:00)

```text
발표용으로 정리해 주세요.
1. 우리 구술자는 누구이고 왜 골랐는가 (2문장)
2. 우리가 답하려 한 질문 한 문장
3. 그래프 규모 — 개체 수·트리플 수·전거 매칭률
4. 그라운딩에서 걸러낸 오류 중 가장 인상적인 것 하나와 그 이유
5. 그래프로 물었을 때와 원문만 읽은 AI에게 물었을 때의 답 차이 (질의 화면 두 탭 비교)
```

발표는 ①어떤 AP를 설계했는지 ②무엇을 그라운딩했는지 ③어떤 화면이 나왔는지 셋입니다.

---

## 3. 막힐 때

| 증상 | 이렇게 말하세요 |
|---|---|
| 올리기에서 문법 오류 | 화면에 뜬 오류 줄을 그대로 그라운더/Claude 에 붙여넣기 |
| 컬렉션이 안 보임 | `.ttl` 에 `rico:RecordSet` 과 소속 술어가 있는지 확인 — 없으면 올릴 때 이름을 적으면 됩니다 |
| 지도·연표가 빔 | 좌표(`geo:lat`/`geo:long`)와 날짜(`rico:beginningDate`)가 그래프에 있는지 확인 |
| 주제가 빔 | 개념(`skos:Concept`) 매핑과 `rico:hasOrHadSubject` 가 있는지 확인 |
| 화면이 안 뜸 | `브라우저 콘솔 에러야: [붙여넣기]. 원인 찾아 줘` |
| SPARQL 0건 | `이 질의가 0건이야: [질의]. 그래프에 실제로 뭐가 있는지 보여 줘` |
| API 오류 | `API 응답이야: [붙여넣기]` — **키 자체는 절대 붙여넣지 말 것** |
