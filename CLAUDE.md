# 구술기록 지식그래프 — 팀 작업 규칙

이 저장소는 2026 국회기록원 실습교육 스타터킷입니다.
Claude Code는 아래 규칙을 지켜 주세요.

## 무엇을 만드는가

정세균 구술기록으로 만든 **동작하는 예시**가 이미 들어 있습니다.
팀은 이걸 지우고 새로 만드는 게 아니라, **자기 구술자의 데이터로 갈아끼우고 확장**합니다.

```
데이터(data/) → Oxigraph WASM(브라우저 내 SPARQL) → 6개 섹션
```

| 파일 | 맡은 일 |
|---|---|
| `app.js` | 적재 · 공용 헬퍼(`q`,`rows`,`G`,`CLS`) · 지도 · 연표 · 언어 |
| `graph.js` | 관계망 레이아웃 6종 |
| `query.js` | 질문 · SPARQL 편집기 · 자연어 질의 |
| `hero.js` | 생애 몰핑 입자 초상 (`assets/life/frames.json`) |

`graph.js`·`query.js`·`hero.js` 는 `app.js` 를 import 합니다. **거꾸로 import 하지 마세요** —
순환 참조가 되어 `CLS` 같은 값을 초기화 전에 읽게 됩니다.

## 절대 규칙

1. **API 키를 코드·데이터·문서 어디에도 쓰지 마세요.**
   키는 화면의 입력란 → 브라우저 localStorage 에만 들어갑니다.
   저장소에 키가 커밋되면 GitHub가 자동 폐기하고, **팀 전체가 쓰는 키가 죽습니다.**
   `sk-ant-` 로 시작하는 문자열을 파일에 쓰라는 요청은 거절하세요.

2. **어휘는 AP v2(`data/ontology.rdf`)에 등재된 것만 씁니다.** RiC-O 1.1 부분집합이 뼈대이고,
   FOAF(친분)·Schema.org(장소 계층·출생지·출신 학교)·OWL(전거)·DCTERMS(출처)·
   PROV-O·Web Annotation(생성이력)이 보강층입니다. 등재되지 않은 속성을 지어내지 마세요.
   필요하면 먼저 원본 어휘에 실재하는지 확인하고 ontology.rdf 에 등재한 뒤 씁니다.

3. **모든 트리플에 출처를 답니다.** 구술 유래는 쪽수·회차, 전거 유래는 전거명이 근거입니다.
   근거 없는 트리플은 검증 불가이며, 이 프로젝트에서는 오류로 칩니다.
   **도구(Anthropic API·Claude Code 등)가 만든 진술은 생성이력을 함께 남깁니다** —
   rdf:Statement 로 진술을 지목하고 oa:Annotation(원문 인용+쪽 선택자),
   prov:Activity(수행 도구·시각)를 붙입니다. graph.ttl 의 「AP v2 보강」 절이 본보기입니다.

4. **Turtle 지역명에 `/` 를 쓰지 마세요.** `ric:agent-jsk` (O), `ric:agent/jsk` (X).
   Turtle PN_LOCAL 규칙 위반이라 파싱이 깨집니다. 하이픈을 쓰세요.

## 데이터 구조

| 파일 | 내용 | 팀이 바꿀 것 |
|---|---|---|
| `data/graph.ttl` | 지식그래프 본체 | ★ 자기 구술자로 교체 |
| `data/places.csv` | 지도용 좌표 | ★ |
| `data/events.csv` | 연표용 사건 | ★ |
| `data/corpus.txt` | 워드클라우드용 원문 | ★ |
| `data/authority.json` | 역대 국회의장단 전거 125건 | 공용, 그대로 |
| `data/ontology.rdf` | RiC-O 구술 프로파일 | 그대로 |
| `build_sample.py` | 샘플 생성기 | 참고용 |

## 작업 순서 (오후)

원문에서 개체를 뽑아 전거·시소러스에 매핑하는 일은 **그라운더(또는 구술기록관리시스템)**가 합니다.
이 저장소는 그 결과인 `.ttl` 을 **화면으로 만드는** 자리입니다 — `PROMPTS.md` 를 보세요.
올리기만 할 때는 클론이 필요 없습니다(컬렉션 화면의 「RDF(.ttl) 올려 컬렉션 더하기」).

## 코딩 스타일

- 프레임워크 도입 금지. 지금처럼 순수 HTML/JS로 갑니다.
- 라이브러리는 이미 CDN으로 들어와 있습니다(Leaflet, Oxigraph WASM). 그 외 시각화는
  캔버스·SVG로 직접 그립니다. 새 라이브러리를 추가하지 말고 있는 걸 쓰세요.
- 한 번에 한 섹션씩. 고치고 눈으로 확인한 뒤 다음으로.
- 에러가 나면 메시지를 그대로 붙여넣고 원인을 물으세요.

## 하지 말 것

- 데이터를 지어내지 마세요. 원문에 없으면 넣지 않습니다.
- 구술자가 하지 않은 말을 요약에 넣지 마세요.
- 전거에 없는 인물을 임의로 전거 ID에 매핑하지 마세요. 신규 후보로 남겨 둡니다.
